<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password - Twitter</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f8fa;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: auto;
            padding: 20px;
        }
        h2 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #1da1f2;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            font-weight: bold;
            display: block;
            margin-bottom: 5px;
        }
        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border-radius: 5px;
            border: 1px solid #ccd0d5;
            box-sizing: border-box;
        }
        button,a {
            background-color: #1da1f2;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0d8bf2;
        }
        a:hover{
            background-color: #0d8bf2;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Change Password</h2>
        <form action="/change_password_submit" method="post">
            <div class="form-group">
                <label for="old_pass">Old Password:</label>
                <input type="password" name="old_pass" required>
            </div>
            <div class="form-group">
                <label for="new_pass">New Password:</label>
                <input type="password" name="new_pass" required>
            </div>
            <div class="form-group">
                <label for="confirm_pass">Confirm Password:</label>
                <input type="password" name="confirm_pass" required>
            </div>
            <button type="submit">Change Password</button>
            <a href="/edit_profile" style="text-decoration: none; color: white;">Go Back</a>
        </form>
        
    </div>
</body>
</html>
